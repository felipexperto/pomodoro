const colors = {
  white: '#fff',
};

const fonts = {
  family: 'Open Sans, sans-serif',
  size: '14px',
  lineHeight: '1.5',
  weight: {
    light: 300,
    normal: 400,
    bold: 600,
    bolder: 700,
  },
};

export const sizes = {
  largeDesktop: 1200,
  desktop: 992,
  tablet: 768,
  phone: 576,
};

const zIndex = {};

const shadows = {};

const theme = {
  main: {
    colors,
    fonts,
    sizes,
    zIndex,
    shadows,
  },
};

export default theme;
